name: Web Components Tests > Keyboard shortcuts for results navigation
steps:
  - step: I have the environment variable "PAGEFIND_SITE" set to "public"
  - step: I have a "public/index.html" file with the content {html}
    html: |-
      <!DOCTYPE html><html lang="en"><head>
      <link href="/pagefind/pagefind-component-ui.css" rel="stylesheet">
      </head><body>
      <pagefind-config preload></pagefind-config>
      <pagefind-input></pagefind-input>
      <pagefind-summary></pagefind-summary>
      <pagefind-results></pagefind-results>
      <script src="/pagefind/pagefind-component-ui.js" type="module"></script>
      </body></html>
  - step: I have a "public/one/index.html" file with the content {html}
    html: >-
      <!DOCTYPE html><html lang="en"><head></head><body><h1>First Result</h1></body></html>
  - step: I have a "public/two/index.html" file with the content {html}
    html: >-
      <!DOCTYPE html><html lang="en"><head></head><body><h1>Second Result</h1></body></html>
  - macro: I run Pagefind
  - step: stdout should contain "Running Pagefind"
  - step: The file "public/pagefind/pagefind.js" should not be empty
  - step: I serve the directory "public"
  - step: In my browser, I load "/"
  - step: In my browser, I click the selector "pagefind-input input"
  - step: In my browser, I type "Result"
  - step: In my browser, the console should be empty
  - step: In my browser, I evaluate {js}
    js: |-
      let results = await toolproof.querySelector("pagefind-results ul");
      await toolproof.waitFor(() => results.querySelectorAll("li a").length === 2);
  - step: In my browser, I press the "ArrowDown" key
  - step: In my browser, I evaluate {js}
    js: |-
      await toolproof.waitFor(() => {
        let anchors = document.querySelectorAll("pagefind-results .pf-result a");
        return anchors.length > 0 && document.activeElement === anchors[0];
      }, "First result should be focused");
  - step: In my browser, I press the "Backspace" key
  - step: In my browser, I evaluate {js}
    js: |-
      let input = await toolproof.querySelector("pagefind-input input");
      await toolproof.waitFor(() => document.activeElement === input);
      toolproof.assert(input.value === "Resul", `Input value should have last char removed, got '${input.value}'`);
  - step: In my browser, I type "t"
  - step: In my browser, I evaluate {js}
    js: |-
      await toolproof.waitFor(() => {
        let anchors = document.querySelectorAll("pagefind-results .pf-result a");
        return anchors.length === 2;
      });
  - step: In my browser, I press the "ArrowDown" key
  - step: In my browser, I evaluate {js}
    js: |-
      await toolproof.waitFor(() => {
        let anchors = document.querySelectorAll("pagefind-results .pf-result a");
        return anchors.length > 0 && document.activeElement === anchors[0];
      }, "Result should be focused before pressing /");
  - step: In my browser, I press the "/" key
  - step: In my browser, I evaluate {js}
    js: |-
      let input = await toolproof.querySelector("pagefind-input input");
      await toolproof.waitFor(() => document.activeElement === input);
  - step: In my browser, I press the "ArrowDown" key
  - step: In my browser, I evaluate {js}
    js: |-
      await toolproof.waitFor(() => {
        let anchors = document.querySelectorAll("pagefind-results .pf-result a");
        return anchors.length > 0 && document.activeElement === anchors[0];
      }, "First result should be focused again");
  - step: In my browser, I press the "x" key
  - step: In my browser, I evaluate {js}
    js: |-
      let input = await toolproof.querySelector("pagefind-input input");
      await toolproof.waitFor(() => document.activeElement === input);
      toolproof.assert(input.value.endsWith('x'), `Input value should end with 'x', got '${input.value}'`);
