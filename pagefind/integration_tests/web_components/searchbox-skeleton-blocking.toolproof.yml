name: Web Components Tests > Searchbox keyboard navigation blocks on skeleton and resumes after load
steps:
  - step: I have the environment variable "PAGEFIND_SITE" set to "public"
  - step: I have a "public/index.html" file with the content {html}
    html: |-
      <!DOCTYPE html><html lang="en"><head>
      <link href="/pagefind/pagefind-component-ui.css" rel="stylesheet">
      </head><body>
      <pagefind-config preload></pagefind-config>
      <pagefind-searchbox></pagefind-searchbox>
      <script src="/pagefind/pagefind-component-ui.js" type="module"></script>
      </body></html>
  - step: I have a "public/one/index.html" file with the content {html}
    html: >-
      <!DOCTYPE html><html lang="en"><head></head><body><h1>First Result</h1><p>Apple content here.</p></body></html>
  - step: I have a "public/two/index.html" file with the content {html}
    html: >-
      <!DOCTYPE html><html lang="en"><head></head><body><h1>Second Result</h1><p>Apple content here too.</p></body></html>
  - step: I have a "public/three/index.html" file with the content {html}
    html: >-
      <!DOCTYPE html><html lang="en"><head></head><body><h1>Third Result</h1><p>More apple content.</p></body></html>
  - step: I have a "public/four/index.html" file with the content {html}
    html: >-
      <!DOCTYPE html><html lang="en"><head></head><body><h1>Fourth Result</h1><p>Extra apple content.</p></body></html>
  - step: I have a "public/five/index.html" file with the content {html}
    html: >-
      <!DOCTYPE html><html lang="en"><head></head><body><h1>Fifth Result</h1><p>Even more apple content.</p></body></html>
  - macro: I run Pagefind
  - step: stdout should contain "Running Pagefind"
  - step: The file "public/pagefind/pagefind.js" should not be empty
  - step: I serve the directory "public"
  - step: In my browser, I load "/"
  - macro: I set up test gates on "pagefind-searchbox" blocking results "2, 3, 4"
  - step: In my browser, I click the selector "pagefind-searchbox input"
  - step: In my browser, I type "apple"
  - step: In my browser, I evaluate {js}
    js: |-
      let results = await toolproof.querySelector(".pf-searchbox-results");
      await toolproof.waitFor(
        () => results.querySelectorAll("a.pf-searchbox-result").length >= 2,
        "At least 2 results should load"
      );
      await toolproof.waitFor(
        () => document.querySelector('.pf-searchbox-results [data-pf-selected]'),
        "First result should be selected"
      );
  - step: In my browser, the console should be empty
  # ArrowDown to second result, then into skeleton
  - step: In my browser, I press the "ArrowDown" key
  - step: In my browser, I evaluate {js}
    js: |-
      let options = document.querySelectorAll('.pf-searchbox-results [role="option"]');
      toolproof.assert(options[1].hasAttribute("data-pf-selected"), "Second option should be selected");
      window.__selectedId = options[1].id;
  - step: In my browser, I press the "ArrowDown" key
  - step: In my browser, I evaluate {js}
    js: |-
      let selected = document.querySelector('.pf-searchbox-results [data-pf-selected]');
      toolproof.assert(
        selected && selected.id === window.__selectedId,
        "Selection should not have moved (blocked on skeleton)"
      );
      let searchbox = document.querySelector("pagefind-searchbox");
      toolproof.assert(
        searchbox.pendingNavigation === 1,
        `Expected pendingNavigation=1, got ${searchbox.pendingNavigation}`
      );
  # Resolve gate — navigation resumes
  - step: In my browser, I evaluate {js}
    js: |-
      window.__testGates[2].resolve();
  - step: In my browser, I evaluate {js}
    js: |-
      await toolproof.waitFor(
        () => {
          let selected = document.querySelector('.pf-searchbox-results [data-pf-selected]');
          return selected && selected.id !== window.__selectedId;
        },
        "Selection should move after skeleton loads"
      );
      let searchbox = document.querySelector("pagefind-searchbox");
      toolproof.assert(
        searchbox.pendingNavigation === 0,
        `Expected pendingNavigation=0 after load, got ${searchbox.pendingNavigation}`
      );
      let selected = document.querySelector('.pf-searchbox-results [data-pf-selected]');
      toolproof.assert(
        selected.getAttribute("aria-selected") === "true",
        "Newly selected option should have aria-selected=true"
      );
