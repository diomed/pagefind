name: Web Components Tests > Searchbox custom templates work correctly
steps:
  - step: I have the environment variable "PAGEFIND_SITE" set to "public"
  - step: I have a "public/index.html" file with the content {html}
    html: |-
      <!DOCTYPE html><html lang="en"><head>
      <link href="/pagefind/pagefind-component-ui.css" rel="stylesheet">
      </head><body>
      <pagefind-config preload></pagefind-config>
      <pagefind-searchbox>
        <script type="text/pagefind-template">
          <a class="custom-result" id="{{ aria.result_id }}" href="{{ meta.url | default(url) | safeUrl }}" role="option" aria-selected="false" aria-labelledby="{{ aria.title_id }}">
            <p class="custom-title" id="{{ aria.title_id }}">{{ meta.title }}</p>
            {{#if excerpt}}
            <p class="custom-excerpt" id="{{ aria.excerpt_id }}">{{+ excerpt +}}</p>
            {{/if}}
          </a>
        </script>
        <script type="text/pagefind-template" data-template="placeholder">
          <div class="custom-placeholder" aria-hidden="true">Loading custom result...</div>
        </script>
      </pagefind-searchbox>
      <script src="/pagefind/pagefind-component-ui.js" type="module"></script>
      </body></html>
  - step: I have a "public/first/index.html" file with the content {html}
    html: >-
      <!DOCTYPE html><html
      lang="en"><head></head><body><h1>First Result</h1><p>This is the first
      result page.</p></body></html>
  - step: I have a "public/second/index.html" file with the content {html}
    html: >-
      <!DOCTYPE html><html
      lang="en"><head></head><body><h1>Second Result</h1><p>This is the second
      result page.</p></body></html>
  - macro: I run Pagefind
  - step: stdout should contain "Running Pagefind"
  - step: The file "public/pagefind/pagefind.js" should not be empty
  - step: I serve the directory "public"
  - step: In my browser, I load "/"
  - step: In my browser, I click the selector "pagefind-searchbox input"
  - step: In my browser, I type "result"
  - step: In my browser, the console should be empty
  # Custom result template should be used
  - step: In my browser, I evaluate {js}
    js: |-
      let results = await toolproof.querySelector(".pf-searchbox-results");
      await toolproof.waitFor(() => results.querySelectorAll(".custom-result").length === 2);
  - snapshot: In my browser, the result of {js}
    js: |-
      let results = await toolproof.querySelector(".pf-searchbox-results");
      let items = [...results.querySelectorAll(".custom-result")];
      items.sort((a, b) => a.querySelector(".custom-title").textContent.localeCompare(b.querySelector(".custom-title").textContent));
      return items.map(el => `${el.querySelector(".custom-title").textContent}: ${el.getAttribute("href")}`).join(", ");
    snapshot_content: |-
      ╎First Result: /first/, Second Result: /second/
